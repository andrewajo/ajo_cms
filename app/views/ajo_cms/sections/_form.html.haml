= form_for @section do |f|
	.modal-body
		.row
			.span3
				.field
					= f.label :name
					= f.text_field :name
			.span2
				.field
					= f.label :title
					= f.text_field :title
	.modal-footer
		= f.submit 'Save', :class => 'btn'
		= link_to sections_path do
			%span.btn Cancel